A writable Mac Mini volume crossed a disk-pressure threshold. Investigate the growth source and return findings in this conversation.

Incident ID: {{INCIDENT_ID}}
Action ID: {{ACTION_ID}}
Stable volume identity: {{VOLUME_ID}}
Mounted from: {{VOLUME_SOURCE}}
Mount path: {{MOUNT_PATH}}
Filesystem: {{FILESYSTEM_TYPE}}
Pressure stage: {{PRESSURE_STAGE}} ({{USED_PERCENT}}% used)
Total bytes: {{TOTAL_BYTES}}
Used bytes: {{USED_BYTES}}
Available bytes: {{AVAILABLE_BYTES}}

Operating rules:

- This is a read-only diagnostic run. Do not delete, move, compress, truncate, or modify any file.
- Never treat ambiguous application data, user data, source repositories, databases, secrets, or generated deliverables as cleanup candidates.
- Inspect this exact mounted volume and identify the largest current paths plus likely recent growth sources using read-only filesystem commands.
- Account for permissions and APFS/shared-container behavior explicitly; do not claim a directory is small merely because it could not be read.
- Separate explicitly safe, reproducible caches from ambiguous or authoritative data. Only describe a cache as safe when an existing repository or storage policy supports that classification, and cite the policy or artifact.
- Do not perform cleanup even when a cache appears safe. Recommend a focused follow-up ticket or user-approved action instead.
- Use recent storage tickets, artifacts, system logs, and prior diagnostic conversations when they help establish what changed.

Return a concise report with: current severity, largest measured sources, likely growth drivers, measurement gaps, policy-supported safe-cache opportunities, data that must not be touched, and recommended next actions.
